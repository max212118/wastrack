
<div class="container-dashboard">
  <div class="cards">
    <div class="card-category">
      <div style="width: 100%; height: 400px;"
          data-controller="mapbox"
          data-mapbox-markers-value="<%= @markers.to_json %>"
          data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
    <div class="card-category" style="width: 70%; height: 400px;">
    </div>

    <div class="card-category" style="width: 70%; height: 400px;">
      <div>
        <ul>
          <%count = 1%>
          <% @trucks.each do |truck| %>
            <li><%= link_to truck.plate_number, truck_path(truck) %></li>
          <%= "team #{count}" %>
          <% count += 1 %>
          <% end %>
        </ul>
      </div>
    </div>

    <div  id="savings-card" class="card-category" style="width: 70%; height: 400px;">
      <div id="savings-card-all">
        <h1 id="savings-card-title" >Economies</h1>
        <div id="savings-card-text">
          <h5> • Co2 :</h5>
          <h5> • Km :</h5>
          <h5> • Argent :</h5>
          <h5> • Temps :</h5>
      </div>
      </div>
    </div>
  </div>
</div>

    <div class="card-category" style="width: 70%; height: 400px;">

<%# list the trucks  %>
<%# link_to a un truck %>
    </div>
  </div>
</div>

<%# trucks.road.km %>


<%# filling rate moyen en fonction des jours %>
<%# map with full screen button%>
<%# savings per day %>
<%# trucks index with show truck %>
